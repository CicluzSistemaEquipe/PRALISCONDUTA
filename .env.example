# Supabase — opcional.
# Sem estas variáveis o app roda 100% no navegador (localStorage).
# Preenchendo-as, todo o progresso/quiz/assinatura passa a usar o Supabase
# automaticamente, sem nenhuma mudança de código.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
