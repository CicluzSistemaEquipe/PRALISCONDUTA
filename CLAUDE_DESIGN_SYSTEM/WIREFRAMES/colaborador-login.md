# Wireframe — Login / Acesso (Colaborador) · 🌙 mobile

```
┌───────────────────────────────┐
│ ❧                          ❧  │  ← folhas-marca sutis (fundo)
│                               │
│                               │
│         Padaria               │
│         pralís                │  ← logotipo da marca
│                               │
│           ╭───────╮           │
│           │  Lis  │👋         │  ← LisAvatar (acenando, ring dourado)
│           ╰───────╯           │
│ ┌───────────────────────────┐ │
│ │ Oi! Para acessar, use o   │ │  ← LisCard (orientação)
│ │ link que o seu gestor     │ │
│ │ enviou para você.         │ │
│ └───────────────────────────┘ │
│  Nenhum código de acesso      │  ← estado (texto muted)
│  detectado.                   │
│                               │
│ ✕                          ✕  │  ← folhas-marca sutis (fundo)
└───────────────────────────────┘
```

**Zonas:** Fundo escuro quente com folhas-marca sutis (cantos) · Logotipo centralizado · LisAvatar (acenando, ring dourado) · LisCard de orientação ("use o link do gestor") · Linha de estado em texto muted (sem código detectado). Sem formulário de senha — acesso é por link.
